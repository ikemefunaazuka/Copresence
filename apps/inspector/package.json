{
  "name": "@copresence/inspector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vue 3 live session inspector — metrics, coalescing ratio, and per-participant convergence, polled from the chaos lab's own JSON API.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7",
    "vue-tsc": "^2.2.0"
  }
}
