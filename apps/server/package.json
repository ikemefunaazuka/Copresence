{
  "name": "@copresence/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Express + ws server for Copresence — MVC, tick-coalesced broadcast, session registry.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@copresence/protocol": "^0.1.0",
    "pino": "^10.3.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.2"
  }
}
